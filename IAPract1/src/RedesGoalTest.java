import aima.search.framework.GoalTest;

/**
 * Class Name: RedesGoalState
 *
 * Description:
 * check is we're at the goal state
 *
 * @author Grupo_IA
 * @version 1.0
 */

public class RedesGoalTest implements GoalTest{

    /**
     * Metodo que comprueba si el estado actual es el objetivo
     *
     * @param state Estado actual
     * @return booleano expresando si el estado es el objetivo
     */
public boolean isGoalState(Object state) {
            return false;
        }
}
